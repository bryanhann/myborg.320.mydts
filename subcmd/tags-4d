name="${PROG} $(basename $0)"
source ${ROOT}/bin/activate
[ "$1" = ""         ] && echo "[$name]: missing argument" && exit 87
[ ! -d "$1"         ] && echo "[$name]: not a directory: [$1]" && exit 88
[ ! -d "$1/.mytags" ] && exit 89

/bin/ls -1 "$1/.mytags"
